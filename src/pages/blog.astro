---
import BaseLayout from "../layouts/BaseLayout.astro";
import BlogPost from "../components/BlogPost.astro";
const allPosts = await Astro.glob("../pages/posts/*.md");
const pageTitle = "My Astro Learning Blog";
---

<BaseLayout pageTitle={pageTitle}>
  <h2>
    This is my main blog page where I will post updates on my game
    development!!!
  </h2>

  <p>I am currently working on a personal RPG project for my portfolio</p>
  <ul>
    {
      allPosts.map((post) => (
        <BlogPost url={post.url} title={post.frontmatter.title} />
      ))
    }
  </ul>
  <p>
    This site will update as I make more progress! So keep checking back and see
    how my journey is going!
  </p>
</BaseLayout>
